@model IEnumerable<AG.Data.Models.Following>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <DisplayName>Follower</DisplayName>
        </th>
        <th>
            <DisplayName>Followee</DisplayName>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.follower.userName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.followee.userName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.follwingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.follwingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.follwingId })
        </td>
    </tr>
}

</table>
